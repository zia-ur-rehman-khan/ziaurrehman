# Roadmap

- [x] Publish portfolio with URL slug `ziaurrehman` — scheduled, expected https://ziaurrehman.lovable.app
- [x] GitHub Pages setup: prerender "/" in vite.config.ts + `.github/workflows/deploy-pages.yml` (builds static site, deploys `.output/public`)
- [x] GitHub connected; Pages enabled with the "GitHub Actions" source
- [ ] Change Lovable URL slug to `portfolio` — cancelled by user
- [x] Verify build-errors.log shows prerender OK after these edits
- [ ] Re-run the GitHub Pages workflow after the repository rename, then verify the `/portfolio/` site loads its styles
